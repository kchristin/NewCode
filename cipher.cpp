#include <iostream>
#include <string>

int main(){

    std::string S;
    int N;
    std::cout << "Enter shift +/- 26 : ";
    std::cin >> N;
    std::cout << "Enter plain one word message (A-Z only) : " ;
    std::cin >> S;

    return 0;
}